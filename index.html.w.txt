<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>HR Vision - Premium Eyewear & Watches</title>
<style>
*{margin:0;padding:0;box-sizing:border-box;font-family:'Segoe UI',sans-serif}
body{background:#0A0A0A;color:#fff;line-height:1.6}
:root{--gold:#D4AF37;--black:#0A0A0A;--dark:#1A1A1A}
header{background:var(--black);border-bottom:1px solid var(--gold);position:sticky;top:0;z-index:100}
.top-bar{background:var(--gold);color:var(--black);text-align:center;padding:8px;font-size:14px;font-weight:600}
nav{max-width:1200px;margin:auto;display:flex;justify-content:space-between;align-items:center;padding:15px 20px}
.logo{font-size:28px;font-weight:800;color:var(--gold);letter-spacing:2px}
.nav-links{display:flex;gap:30px}
.nav-links a{color:#fff;text-decoration:none;font-weight:500;transition:.3s}
.nav-links a:hover{color:var(--gold)}
.hero{background:linear-gradient(rgba(0,0,0,.7),rgba(0,0,0,.7)),url('https://images.unsplash.com/photo-1511499767150-a48a237f0083?w=1200') center/cover;height:70vh;display:flex;align-items:center;justify-content:center;text-align:center}
.hero h1{font-size:48px;color:var(--gold);margin-bottom:20px;animation:fadeIn 1s}
.hero p{font-size:20px;margin-bottom:30px}
.btn{background:var(--gold);color:var(--black);padding:14px 35px;border:none;font-weight:700;cursor:pointer;transition:.3s;text-decoration:none;display:inline-block}
.btn:hover{background:#fff;transform:translateY(-2px)}
.container{max-width:1200px;margin:50px auto;padding:0 20px}
.filters{display:flex;gap:15px;justify-content:center;margin-bottom:40px;flex-wrap:wrap}
.filter-btn{background:var(--dark);color:#fff;border:1px solid var(--gold);padding:10px 25px;cursor:pointer;transition:.3s}
.filter-btn.active,.filter-btn:hover{background:var(--gold);color:var(--black)}
.products{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:30px}
.product{background:var(--dark);border:1px solid #333;overflow:hidden;transition:.4s;position:relative}
.product:hover{transform:translateY(-10px);border-color:var(--gold);box-shadow:0 10px 30px rgba(212,175,55,.2)}
.product-img{width:100%;height:320px;object-fit:cover;transition:.5s}
.product:hover .product-img{transform:scale(1.1)}
.product-info{padding:20px}
.product-title{font-size:18px;margin-bottom:8px}
.product-price{color:var(--gold);font-size:22px;font-weight:700;margin-bottom:15px}
.product-btn{width:100%;background:transparent;border:1px solid var(--gold);color:var(--gold);padding:12px}
.product-btn:hover{background:var(--gold);color:var(--black)}
.badge{position:absolute;top:15px;right:15px;background:var(--gold);color:var(--black);padding:5px 12px;font-size:12px;font-weight:700}
#adminPanel{display:none;background:var(--dark);padding:30px;margin:40px 0;border:1px solid var(--gold)}
#adminPanel input,#adminPanel select{width:100%;padding:12px;margin:8px 0;background:#000;border:1px solid #333;color:#fff}
.whatsapp-float{position:fixed;bottom:30px;right:30px;background:#25D366;width:60px;height:60px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:30px;z-index:999;box-shadow:0 5px 15px rgba(0,0,0,.5)}
footer{background:var(--black);border-top:1px solid var(--gold);text-align:center;padding:30px 20px;margin-top:60px}
@keyframes fadeIn{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}
@media(max-width:768px){.hero h1{font-size:32px}.nav-links{display:none}}
</style>
</head>
<body>
<div class="top-bar">Free Delivery in Lahore | COD & JazzCash Available | WhatsApp: 03284854373</div>
<header>
<nav>
<div class="logo">HR VISION</div>
<div class="nav-links">
<a href="#home">Home</a>
<a href="#shop">Shop</a>
<a href="#" onclick="toggleAdmin()">Admin</a>
</div>
</nav>
</header>

<section class="hero" id="home">
<div>
<h1>LUXURY EYEWEAR & WATCHES</h1>
<p>Premium Collection | Cash on Delivery Available</p>
<a href="#shop" class="btn">SHOP NOW</a>
</div>
</section>

<div class="container" id="shop">
<div class="filters">
<button class="filter-btn active" onclick="filterProducts('all')">All Products</button>
<button class="filter-btn" onclick="filterProducts('Glasses')">Glasses</button>
<button class="filter-btn" onclick="filterProducts('Watches')">Watches</button>
</div>

<div id="adminPanel">
<h3 style="color:var(--gold);margin-bottom:20px">Add New Product</h3>
<input type="text" id="pName" placeholder="Product Name">
<input type="number" id="pPrice" placeholder="Price in PKR">
<input type="text" id="pImg" placeholder="Image URL 1">
<input type="text" id="pImg2" placeholder="Image URL 2">
<select id="pType"><option>Glasses</option><option>Watches</option></select>
<button class="btn" onclick="addProduct()" style="margin-top:10px">Add Product</button>
</div>

<div class="products" id="productList"></div>
</div>

<a href="https://wa.me/923284854373?text=Hi%20HR%20Vision,%20I%20want%20to%20order" class="whatsapp-float">💬</a>

<footer>
<p style="color:var(--gold);font-size:20px;font-weight:700;margin-bottom:10px">HR VISION</p>
<p>Premium Eyewear & Watches Store - Lahore</p>
<p style="margin-top:15px">WhatsApp: 03284854373 | COD Available All Over Pakistan</p>
</footer>

<script>
let products = JSON.parse(localStorage.getItem('hrProducts')) || [];
let cart = [];

function saveProducts(){localStorage.setItem('hrProducts',JSON.stringify(products))}
function renderProducts(filter='all'){
let list=document.getElementById('productList');list.innerHTML='';
let filtered=filter==='all'?products:products.filter(p=>p.type===filter);
if(filtered.length===0){list.innerHTML='<p style="text-align:center;grid-column:1/-1">No products yet. Add from Admin Panel.</p>';return}
filtered.forEach((p,i)=>{
list.innerHTML+=`<div class="product">
<span class="badge">NEW</span>
<img src="${p.img}" class="product-img" alt="${p.name}">
<div class="product-info">
<h3 class="product-title">${p.name}</h3>
<p class="product-price">Rs. ${p.price}</p>
<button class="product-btn" onclick="orderNow('${p.name}',${p.price})">Order on WhatsApp</button>
</div></div>`
})}

function filterProducts(type){
document.querySelectorAll('.filter-btn').forEach(b=>b.classList.remove('active'));
event.target.classList.add('active');renderProducts(type)}

function toggleAdmin(){
let panel=document.getElementById('adminPanel');
panel.style.display=panel.style.display==='none'||panel.style.display===''? 'block':'none'}

function addProduct(){
let name=document.getElementById('pName').value;
let price=document.getElementById('pPrice').value;
let img=document.getElementById('pImg').value;
let type=document.getElementById('pType').value;
if(!name||!price||!img){alert('Fill all fields');return}
products.push({name,price,img,type});saveProducts();renderProducts();
document.getElementById('pName').value='';document.getElementById('pPrice').value='';document.getElementById('pImg').value='';
alert('Product Added!')}

function orderNow(name,price){
let msg=`Hi HR Vision,%0A%0AI want to order:%0A*Product:* ${name}%0A*Price:* Rs. ${price}%0A%0AName:%0AAddress:%0ACity:%0APayment: COD/JazzCash`;
window.open(`https://wa.me/923284854373?text=${msg}`,'_blank')}

renderProducts();
</script>
</body>
</html>